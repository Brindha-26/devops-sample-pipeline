version: 0.1
component: build

timeoutInSeconds: 600

steps:
  - type: Command
    name: Debug
    command: |
      echo "Listing files"
      ls -la

  - type: Command
    name: Build Project
    command: |
      mvn clean compile

outputArtifacts:
  - name: build_output
    type: BINARY
    location: target
