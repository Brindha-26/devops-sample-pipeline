version: 0.1
component: build

timeoutInSeconds: 600

steps:
  - type: Command
    name: Build JavaFX Project
    command: |
      echo "Building project"
      mvn clean package -DskipTests

outputArtifacts:
  - name: build_output
    type: BINARY
    location: target
